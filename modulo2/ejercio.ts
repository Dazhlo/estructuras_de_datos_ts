// Tu Ejercicio Práctico: El reto "Two Sum"Este es, 
// literalmente, el problema número 1 que hacen las grandes 
// empresas (Google, Amazon, Meta) en sus entrevistas iniciales.
// El Reto:Te doy un array de números enteros y un número objetivo. 
// Tienes que devolver los índices de los dos números que sumados den el objetivo

// ejemplo 

// array = [2, 7, 11, 15]
// objetivo = 9
// Respuesta esperada: [0, 1] (Porque el array en la posición 0 vale 2, y en la posición 1 vale 7. 2 + 7 = 9).

class tableHast<T> {
    private memoria: Array<T>
    constructor() {
        this.memoria = new Array;
    }

    private recorrer() {
        let numero;
        let numero1;
        for (let i = 0; i <= this.memoria.length; i++) {
            if (i === 2)
                numero = this.memoria.length;

        }

    }


}